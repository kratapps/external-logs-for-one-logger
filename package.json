{
  "name": "external-logs-for-one-logger",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "husky install",
    "prettier-all": "prettier --write {,*/**/}*.{md,cls,trigger,json}",
    "prettier-staged": "lint-staged"
  },
  "devDependencies": {
    "@kratapps/prettier-config": "^0.1.5",
    "husky": "^7.0.4",
    "lint-staged": "^12.5.0",
    "prettier": "^2.7.1"
  },
  "lint-staged": {
    "**/*.{md,cls,json}": [
      "prettier --write"
    ]
  }
}
